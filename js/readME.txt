1. console.log(checkDiff(array1.slice(0, array1.length - 1), array2.slice(0, array2.length - 1))); -
возвращает массив с повторяющимися словами

2. console.log(refactorArr(array1, array2)); - возвращает весь словарь (base) с замененными повторениями